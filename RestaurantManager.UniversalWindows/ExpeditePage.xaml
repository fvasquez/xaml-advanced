<Page
    x:Class="RestaurantManager.UniversalWindows.ExpeditePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RestaurantManager.UniversalWindows"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid x:Name="ExpediteGrid" DataContext="{StaticResource DataManager}"
          Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="8*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <AppBar>
            <AppBarButton x:Name="HomeButton" Click="HomeButton_Click" Icon="Home" Label="Home"/>
        </AppBar>
        <GridView x:Name="OrderItemsGridView" ItemsSource="{Binding OrderItems}" Grid.Row="1">
        </GridView>
        <Button x:Name="ClearOrdersButton" Content="Clear All Orders" Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center" Click="ClearOrdersButton_Click"/>
    </Grid>
</Page>
